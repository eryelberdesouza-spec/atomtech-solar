{
  "name": "atomtech-solar",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "concurrently \"npm run dev -w apps/api\" \"npm run dev -w apps/web\" \"npm run dev -w apps/financeiro\"",
    "dev:propostas": "concurrently \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "dev:financeiro": "concurrently \"npm run dev -w apps/api\" \"npm run dev -w apps/financeiro\"",
    "build": "npm run build -w apps/api && npm run build -w apps/web && npm run build -w apps/financeiro",
    "db:generate": "npm run db:generate -w apps/api",
    "db:migrate": "npm run db:migrate -w apps/api",
    "db:seed": "npm run db:seed -w apps/api"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
